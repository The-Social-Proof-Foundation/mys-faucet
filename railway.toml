[build]
builder = "dockerfile"
# NOTE: Railway MUST be configured in the UI to use repository root as service root directory
# Go to Railway Dashboard -> Service Settings -> Root Directory -> Set to "/" or repository root
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/app/bin/setup-wallet.sh"
