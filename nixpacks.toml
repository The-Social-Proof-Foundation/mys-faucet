providers = ["rust"]

[phases.build]
cmds = [
  'mkdir -p bin',
  'cargo build --release --target x86_64-unknown-linux-musl -p mys-faucet',
  'cp target/x86_64-unknown-linux-musl/release/mys-faucet bin/',
  'chmod +x bin/mys-faucet'
]

[start]
cmd = './bin/mys-faucet'