{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
      "builder": "RAILPACK",
      "buildCommand": "mkdir -p bin && cargo build --release && cp target/release/mys-faucet bin/ && cp target/release/merge_coins bin/"
    },
    "deploy": {
      "startCommand": "./bin/mys-faucet"
    }
}